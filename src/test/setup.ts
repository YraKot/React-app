import { beforeAll, afterAll } from 'vitest';
import '@testing-library/jest-dom';

// Global vitest setup
beforeAll(() => {
  // Setup any global test environment configurations here
});

afterAll(() => {
  // Cleanup any global test environment configurations here
});
